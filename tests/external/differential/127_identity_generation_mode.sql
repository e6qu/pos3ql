CREATE TABLE identity_generation_mode_rows (
  id integer GENERATED BY DEFAULT AS IDENTITY,
  payload text
);
INSERT INTO identity_generation_mode_rows (id, payload) VALUES (40, 'before');
ALTER TABLE identity_generation_mode_rows ALTER COLUMN id SET GENERATED ALWAYS;
SELECT attidentity
  FROM pg_attribute
 WHERE attrelid = 'identity_generation_mode_rows'::regclass
   AND attname = 'id';
ALTER TABLE identity_generation_mode_rows ALTER COLUMN id SET GENERATED BY DEFAULT;
INSERT INTO identity_generation_mode_rows (id, payload) VALUES (41, 'after');
SELECT id, payload FROM identity_generation_mode_rows ORDER BY id;
DROP TABLE identity_generation_mode_rows;
