# Startup capacities required by the shared curated differential corpus.
max_savepoints_per_transaction = 32
max_ddl_per_transaction = 512
max_deferred_constraints_per_transaction = 1024
deferred_trigger_bytes = 1MiB
